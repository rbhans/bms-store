[package]
name = "bms-store-domain"
version = "0.1.0"
edition = "2021"
description = "Wire DTOs for the bms-store HTTP and WebSocket APIs"
license = "MIT OR Apache-2.0"

[dependencies]
bms-core = { path = "../bms-core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
