[package]
name = "bms-haystack"
version = "0.1.0"
edition = "2021"
description = "Project Haystack 5 / Xeto ontology + tagging for bms-store"
license = "MIT OR Apache-2.0"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
