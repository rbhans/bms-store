// bms-store service worker placeholder. The setup GUI will replace this with
// the real desktop/web asset pipeline in Phase 6.
self.addEventListener('install', (event) => {
  self.skipWaiting();
});
